# Security

Time Zone Switcher changes the macOS system time zone only after the user toggles the switch and approves the macOS administrator prompt.

The app does not collect analytics, does not send network requests, and does not run background polling jobs.

If you find a security issue, please open a GitHub issue with enough detail to reproduce the behavior.
